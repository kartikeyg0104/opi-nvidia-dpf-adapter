---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: nodeserviceinterfaces.svc.dpu.nvidia.com
spec:
  group: svc.dpu.nvidia.com
  names:
    kind: NodeServiceInterfaces
    listKind: NodeServiceInterfacesList
    plural: nodeserviceinterfaces
    singular: nodeserviceinterfaces
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.node
      name: Node
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NodeServiceInterfaces is the Schema for the nodeserviceinterfaces
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NodeServiceInterfacesSpec defines the desired state of NodeServiceInterfaces.
            properties:
              interfaces:
                description: Interfaces is the list of service interface entries for
                  this node.
                items:
                  description: InterfaceEntry defines a single service interface entry
                    within a NodeServiceInterfaces object.
                  properties:
                    annotations:
                      additionalProperties:
                        type: string
                      description: Annotations carry operational metadata for this
                        entry.
                      maxProperties: 50
                      type: object
                    interfaceType:
                      description: InterfaceType is the type of the interface.
                      enum:
                      - vlan
                      - physical
                      - pf
                      - vf
                      - ovn
                      - patch
                      - service
                      type: string
                    labels:
                      additionalProperties:
                        type: string
                      description: Labels used for ServiceChain matchLabels resolution.
                      maxProperties: 50
                      type: object
                    name:
                      description: |-
                        Name uniquely identifies this entry within the NodeServiceInterfaces object.
                        Format: `<namespace>_<service-interface-set-name>`. The underscore separator is collision-free
                        because Kubernetes namespace and resource names follow DNS subdomain rules
                        and can never contain an underscore. Both components are
                        bounded to 63 chars, so the full name is at most 127 chars.
                      maxLength: 253
                      minLength: 1
                      type: string
                    ovn:
                      description: OVN is the OVN definition.
                      properties:
                        externalBridge:
                          default: br-ovn
                          description: ExternalBridge is the name of the OVN bridge
                          type: string
                      type: object
                    patch:
                      description: Patch is the patch definition.
                      properties:
                        peerBridge:
                          description: |-
                            PeerBridge is the name of the bridge to which the patch port is connected.
                            This bridge must be created before the ServiceInterface is created.
                          type: string
                        peerExternalIDs:
                          additionalProperties:
                            type: string
                          description: PeerExternalIDs are the external IDs used to
                            identify the peer patch port.
                          type: object
                        peerPatchName:
                          description: |-
                            PeerPatchName is the name of the patch port on the peer bridge.
                            If not set, it is auto-generated in the format: `p_<bridgeA>_to_<bridgeB>_<hash>`
                            where bridge names have hyphens removed and `<hash>` is an 8-character FNV-1a hash
                            derived from the ServiceInterface's namespace/name.
                            Example: p_brovn_to_brsfc_7aea60f7 (for bridges br-ovn and br-sfc).
                          type: string
                      required:
                      - peerBridge
                      type: object
                    pf:
                      description: PF is the PF definition.
                      properties:
                        nicSelector:
                          description: NICSelector defines the NIC selected for the
                            PF interface
                          properties:
                            controllerNumber:
                              description: |-
                                ControllerNumber specifies the controller number that will be used to find the matching representor on the DPU.
                                A value of 0 targets the local controller, >=1 targets external controllers with the specified number. If unspecified controller number 1 is used.
                                In case of a DPU/NIC with socket direct or MultiHost, the user should specify the controller number explicitly
                              format: int32
                              maximum: 255
                              minimum: 0
                              type: integer
                            pci:
                              description: PCI is the PCI selector. valid only for
                                selector type pci
                              properties:
                                address:
                                  description: Address is the PCI address of any of
                                    the NIC's Embedded CPU PFs (ECPFs)
                                  pattern: ^[0-9a-f]{4}:[0-9a-f]{2}:[0-1][0-9a-f]\.[0-7]{1}$
                                  type: string
                              required:
                              - address
                              type: object
                            type:
                              description: Type is the type of selector to be used
                                to identify the NIC.
                              enum:
                              - dpu
                              - pci
                              type: string
                          required:
                          - type
                          type: object
                          x-kubernetes-validations:
                          - message: for type=pci, pci must be set
                            rule: (self.type == 'dpu') || (self.type == 'pci' && has(self.pci))
                        pfID:
                          description: The PF ID
                          maximum: 255
                          minimum: 0
                          type: integer
                        virtualNetwork:
                          description: VirtualNetwork is the VirtualNetwork name in
                            the same namespace
                          type: string
                      required:
                      - pfID
                      type: object
                    physical:
                      description: Physical is the physical interface definition.
                      properties:
                        interfaceName:
                          description: The interface name
                          type: string
                      required:
                      - interfaceName
                      type: object
                    service:
                      description: Service is the service definition.
                      properties:
                        interfaceName:
                          description: The interface name
                          maxLength: 15
                          minLength: 1
                          type: string
                        network:
                          description: |-
                            Network is the Network Attachment Definition in the form of "namespace/name"
                            or just "name" if the namespace is the same as the ServiceInterface.
                          type: string
                        serviceID:
                          description: ServiceID is the DPU Service Identifier
                          type: string
                        virtualNetwork:
                          description: VirtualNetwork is the VirtualNetwork name in
                            the same namespace
                          type: string
                      required:
                      - interfaceName
                      - network
                      - serviceID
                      type: object
                    terminating:
                      description: |-
                        Terminating indicates this entry is marked for removal. Cleanup may
                        require one or more reconcilers. ResourceReleased=True is set only
                        after all required cleanup conditions are satisfied in status.
                      type: boolean
                    vf:
                      description: VF is the VF definition.
                      properties:
                        nicSelector:
                          description: NICSelector defines the NIC selected for the
                            VF interface
                          properties:
                            controllerNumber:
                              description: |-
                                ControllerNumber specifies the controller number that will be used to find the matching representor on the DPU.
                                A value of 0 targets the local controller, >=1 targets external controllers with the specified number. If unspecified controller number 1 is used.
                                In case of a DPU/NIC with socket direct or MultiHost, the user should specify the controller number explicitly
                              format: int32
                              maximum: 255
                              minimum: 0
                              type: integer
                            pci:
                              description: PCI is the PCI selector. valid only for
                                selector type pci
                              properties:
                                address:
                                  description: Address is the PCI address of any of
                                    the NIC's Embedded CPU PFs (ECPFs)
                                  pattern: ^[0-9a-f]{4}:[0-9a-f]{2}:[0-1][0-9a-f]\.[0-7]{1}$
                                  type: string
                              required:
                              - address
                              type: object
                            type:
                              description: Type is the type of selector to be used
                                to identify the NIC.
                              enum:
                              - dpu
                              - pci
                              type: string
                          required:
                          - type
                          type: object
                          x-kubernetes-validations:
                          - message: for type=pci, pci must be set
                            rule: (self.type == 'dpu') || (self.type == 'pci' && has(self.pci))
                        parentInterfaceRef:
                          description: The parent interface reference
                          type: string
                        pfID:
                          description: The PF ID
                          maximum: 255
                          minimum: 0
                          type: integer
                        vfID:
                          description: The VF ID
                          maximum: 255
                          minimum: 0
                          type: integer
                        virtualNetwork:
                          description: VirtualNetwork is the VirtualNetwork name in
                            the same namespace
                          type: string
                      required:
                      - pfID
                      - vfID
                      type: object
                    vlan:
                      description: Vlan is the VLAN definition.
                      properties:
                        parentInterfaceRef:
                          description: The parent interface reference
                          type: string
                        vlanID:
                          description: The VLAN ID
                          type: integer
                      required:
                      - parentInterfaceRef
                      - vlanID
                      type: object
                  required:
                  - interfaceType
                  - name
                  type: object
                  x-kubernetes-validations:
                  - messageExpression: '''for interfaceType='' + self.interfaceType
                      + '', '' + self.interfaceType + '' must be set'''
                    rule: (self.interfaceType == 'vlan' && has(self.vlan)) || (self.interfaceType
                      == 'pf' && has(self.pf)) || (self.interfaceType == 'vf' && has(self.vf))
                      || (self.interfaceType == 'physical' && has(self.physical))
                      || (self.interfaceType == 'service' && has(self.service)) ||
                      (self.interfaceType == 'ovn') || (self.interfaceType == 'patch'
                      && has(self.patch))
                maxItems: 256
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              node:
                description: Node is the name of the DPU node this object represents.
                maxLength: 253
                minLength: 1
                type: string
              type:
                description: |-
                  Type identifies which controller domain owns this NSI shard.
                  Examples: "sfc", "vpc-my-provisioner".
                maxLength: 253
                minLength: 1
                type: string
            required:
            - node
            - type
            type: object
            x-kubernetes-validations:
            - message: node is immutable
              rule: self.node == oldSelf.node
            - message: type is immutable
              rule: self.type == oldSelf.type
          status:
            description: NodeServiceInterfacesStatus defines the observed state of
              NodeServiceInterfaces.
            properties:
              conditions:
                description: Conditions reflect aggregate status.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              interfaceStatuses:
                description: |-
                  InterfaceStatuses tracks per-entry reconciliation state. Written by
                  the managing controller. The params map carries controller-specific
                  release information needed during terminating/release handling.
                items:
                  description: InterfaceEntryStatus records reconciliation state for
                    a single interface entry.
                  properties:
                    conditions:
                      description: Conditions may be written by multiple reconcilers
                        for the same entry.
                      items:
                        description: Condition contains details for one aspect of
                          the current state of this API Resource.
                        properties:
                          lastTransitionTime:
                            description: |-
                              lastTransitionTime is the last time the condition transitioned from one status to another.
                              This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                            format: date-time
                            type: string
                          message:
                            description: |-
                              message is a human readable message indicating details about the transition.
                              This may be an empty string.
                            maxLength: 32768
                            type: string
                          observedGeneration:
                            description: |-
                              observedGeneration represents the .metadata.generation that the condition was set based upon.
                              For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                              with respect to the current state of the instance.
                            format: int64
                            minimum: 0
                            type: integer
                          reason:
                            description: |-
                              reason contains a programmatic identifier indicating the reason for the condition's last transition.
                              Producers of specific condition types may define expected values and meanings for this field,
                              and whether the values are considered a guaranteed API.
                              The value should be a CamelCase string.
                              This field may not be empty.
                            maxLength: 1024
                            minLength: 1
                            pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                            type: string
                          status:
                            description: status of the condition, one of True, False,
                              Unknown.
                            enum:
                            - "True"
                            - "False"
                            - Unknown
                            type: string
                          type:
                            description: type of condition in CamelCase or in foo.example.com/CamelCase.
                            maxLength: 316
                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                            type: string
                        required:
                        - lastTransitionTime
                        - message
                        - reason
                        - status
                        - type
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - type
                      x-kubernetes-list-type: map
                    name:
                      description: Name matches the spec entry name.
                      minLength: 1
                      type: string
                    params:
                      additionalProperties:
                        type: string
                      description: |-
                        Params carries controller-specific information for handling reconcile/release of the matching InterfaceEntry in spec.
                        Multiple reconcilers may write distinct keys safely as long as they do not share keys.
                      type: object
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the last observed generation of
                  the NodeServiceInterfaces object.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
